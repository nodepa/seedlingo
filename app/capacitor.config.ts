import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.nodepa.seedling',
  appName: '种字',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
